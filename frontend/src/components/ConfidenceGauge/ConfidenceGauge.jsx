import './ConfidenceGauge.css'; export default function ConfidenceGauge({confidence}){if(!confidence)return null; const {score,level,components,explanation}=confidence;return <div className={`confidence ${level}`}><div className="gauge" style={{'--score':`${score*3.6}deg`}}><b>{score}%</b></div><div><h3>{level} confidence</h3><p>{explanation}</p><details><summary>Score breakdown</summary>{Object.entries(components).map(([k,v])=><div className="metric" key={k}><span>{k.replaceAll('_',' ')}</span><b>{Math.round(v*100)}%</b></div>)}</details></div></div>}
