import './CitationCard.css'; export default function CitationCard({citation,index}){return <details className="citation"><summary><span>p. {citation.page_number}</span><div><b>{citation.manual_name}</b><small>{citation.section_title}</small></div><strong>{Math.round(citation.retrieval_score*100)}%</strong></summary><p>{citation.excerpt}</p><small>Reranker relevance: {Math.round(citation.reranker_score*100)}%</small></details>}
