import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {ShieldCheck} from 'lucide-react';import {useAuth} from '../../context/AuthContext';import './Auth.css';
export default function Auth({mode}){const registerMode=mode==='register';const [form,setForm]=useState({full_name:'',email:'',password:''}),[error,setError]=useState(''),[busy,setBusy]=useState(false);const auth=useAuth(),navigate=useNavigate();const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await (registerMode?auth.register(form):auth.login({email:form.email,password:form.password}));navigate('/app')}catch(err){setError(err.response?.data?.detail||'Authentication failed')}finally{setBusy(false)}};return <main className="auth-page"><Link className="auth-brand" to="/"><i>EA</i>EquipAssist AI</Link><section className="auth-card"><div className="auth-icon"><ShieldCheck/></div><p className="eyebrow">SECURE DIAGNOSTIC ACCESS</p><h1>{registerMode?'Create your account':'Welcome back'}</h1><p className="muted">{registerMode?'Register to manage manuals and troubleshooting sessions.':'Sign in to continue your equipment troubleshooting session.'}</p><form onSubmit={submit}>{registerMode&&<label>Full name<input required minLength="2" value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label>}<label>Email address<input required type="email" autoComplete="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input required minLength="8" type="password" autoComplete={registerMode?'new-password':'current-password'} value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<p className="auth-error">{error}</p>}<button className="btn btn-primary" disabled={busy}>{busy?'Please wait…':registerMode?'Create account':'Sign in'}</button></form><p>{registerMode?'Already have an account?':'New to EquipAssist?'} <Link to={registerMode?'/login':'/register'}>{registerMode?'Sign in':'Create account'}</Link></p></section></main>}
